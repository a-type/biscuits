{
	"name": "@worker.biscuits/domain-route-proxy",
	"private": true,
	"type": "module",
	"scripts": {
		"dev": "wrangler dev --local --port 6400 --persist-to ../core/.wrangler/state ./src/index.ts",
		"build": "wrangler build",
		"deploy": "wrangler deploy",
		"types": "wrangler types -c wrangler.jsonc",
		"postinstall": "pnpm run types"
	},
	"dependencies": {
		"@biscuits/apps": "workspace:*",
		"@biscuits/domain-routes": "workspace:*",
		"cookie": "catalog:"
	},
	"devDependencies": {
		"wrangler": "catalog:"
	}
}
