/*
Auto-generated by: https://github.com/pmndrs/gltfjsx
*/

import { useGLTF } from '@react-three/drei';
import { useRef } from 'react';
import * as THREE from 'three';
// @ts-ignore
import { animated } from '@react-spring/three';
import { GLTF } from 'three-stdlib';
import { purple, white } from '../materials.js';

type GLTFResult = GLTF & {
	nodes: {
		Mesh_onionHalf: THREE.Mesh;
		Mesh_onionHalf_1: THREE.Mesh;
	};
	materials: {
		brownLight: THREE.MeshStandardMaterial;
		brown: THREE.MeshStandardMaterial;
	};
};

export function OnionHalf({ ...props }: JSX.IntrinsicElements['group']) {
	const group = useRef<THREE.Group>(null);
	const { nodes, materials } = useGLTF(
		'/models/kenney-foods/smoothed/onionHalf.glb',
	) as GLTFResult;
	return (
		<animated.group ref={group} {...props} dispose={null}>
			<mesh
				castShadow
				geometry={nodes.Mesh_onionHalf.geometry}
				material={white}
			/>
			<mesh
				castShadow
				geometry={nodes.Mesh_onionHalf_1.geometry}
				material={purple}
			/>
		</animated.group>
	);
}

// useGLTF.preload('/models/kenney-foods/smoothed/onionHalf.glb');
